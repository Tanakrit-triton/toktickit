// Placeholder. Issue #17 replaces this file entirely.
// Deliberately an h1 and a test hook and nothing else: no layout, no state, no
// API call. Anything more would be work thrown away, and would risk #17
// inheriting a shape meant to be discarded.
export function MyTickets() {
  return <h1 data-testid="my-tickets-screen">My Tickets</h1>;
}
